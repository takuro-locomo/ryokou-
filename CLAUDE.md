# CLAUDE.md — ryokou（旅行計画）

## このリポジトリの役割
旅行の日程検討・候補比較・予約控えをまとめる。アプリではなくドキュメント中心。

## 作業ルール
- 最初に `00_要件.md` を確認し、未定項目があればユーザーに聞く（推測で日程を確定しない）
- 日程案は `02_日程案/案X_YYYY-MM-DD.md` の形式で複数案を並べ、比較表を付けて提示する
- 移動時間・料金・営業日は必ず調べた日付と出典URLを添える（変わりやすいため）
- 予約は絶対に代行しない。予約ページのURLと入力内容の案までを用意し、実行はユーザーが行う
- 予約番号・氏名・カード情報・パスポート番号などはコミットしない
- コミットは日本語メッセージ、触ったファイルだけ明示ステージ（`git add -A` 禁止）

## 日程案の書式（`02_日程案/`）
- 1日1見出し（`## Day1 M/D(曜)`）
- 時刻 / 場所 / 移動手段・所要 / 費用目安 / メモ の表
- 末尾に「この案の長所・短所」「未確認事項」
